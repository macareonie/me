import { useMALData } from "../context/MALDataContext";

// hook logic moved to MALDataContext to allow for loading of MAL data the moment the App is accessed and not just when the Ratings page is visited
export { useMALData };
